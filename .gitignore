# 認証情報は絶対にコミットしない（Phase 2b で Drive から実行時に読む方針）
*credential*
*.cred
*.cred.b64
runner/.env
runner/**/secret*
node_modules/
runner/.browser-profile/

# scratch clone used by the scheduled news-digest routine to build & push
# directly to origin/main (see editorial-spec.md) — not part of this session branch
/repo/
