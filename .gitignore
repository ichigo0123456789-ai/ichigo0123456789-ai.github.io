# 認証情報は絶対にコミットしない（Phase 2b で Drive から実行時に読む方針）
*credential*
*.cred
*.cred.b64
runner/.env
runner/**/secret*
node_modules/
runner/.browser-profile/

# scheduled news-digest routine: scratch clone used to push directly to main
/repo/
